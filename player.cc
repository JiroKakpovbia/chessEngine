#include "player.h"
#include <string>
#include <vector>

string Player::getColour() {
    return colour;
}

std::vector<string> Player::getMove(Board &board) {
    std::vector<string> empty;
    return empty;
}
